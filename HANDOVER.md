# Handover — 2026-08-22 09:10

## Goal

**WEB OPS**: a standalone digital facility operations & compliance platform (checklists, 5S, photo evidence, 3-level verification, corrective actions, area release, audit trail) for the user's manufacturing company. Deliberately **separate** from her other apps (BlendCaps/eagle-labs-schedule, Fudgee, etc.) — do not cross-reference or merge them. Two threads running in parallel across sessions:
1. The generic platform itself (schema, auth, RBAC, admin tools, full CRUD everywhere).
2. Digitizing her **real** controlled documents (Capsule Room's cleaning forms, the 5S Daily Check) so they replace paper — not demo filler.

## State — live on production as of this write-up

- Local repo: `~/webops-platform`. Pushed to GitHub: `dhanu-af/webops-platform` (private). **Working tree clean, nothing uncommitted, nothing unpushed** (`git status` / `git log origin/master..HEAD` both confirm this at time of writing, `HEAD` = `a05ec87`).
- **Live production**: Vercel team `DKNS` (`dkns1`) → project `webops-platform` → **https://webops-platform-three.vercel.app**. Neon Postgres `neon-byzantium-saddle` + Vercel Blob. Auto-deploy-on-push confirmed working reliably all session.
- The admin platform is now feature-complete for everything identified so far: full CRUD on Facility/Section/Area/Equipment, Users, Checklists (+ cloning), Verification Workflows, System Settings (branding/photo limits/notifications), Audit Trail viewer, Reports (CSV+PDF export), Analytics, Calendar. Every placeholder page from the original build is done. Every "read-only page missing CRUD" gap found by repeated fresh-eyes re-audits has been closed (Users, Areas/Equipment, Facility/Section, Checklist Builder permission gates, Verification Workflow builder).
- **Real controlled-document digitization in progress**: Capsule Room's cleaning checklists (from earlier sessions) plus the **5S Daily Check** — a real paper form (5 sections: Sort/Set in Order/Shine/Standardize/Sustain, 27 items, 0/1/2 compliance scoring) rolled out per-area via the checklist-cloning feature.
- **RESOLVED today**: "5S Daily Check - Capsule Room" now has a live `DAILY · Capsule Room · SUPERVISOR` schedule (added via the live admin UI, no code change) and appears correctly on `/five-s`. Went with area-specific scoping (the more literal reading of a per-area clone) rather than facility-wide.
- **Still open**: "5S Daily Check - Blending Room"'s existing schedule remains scoped to the **whole facility**, not Blending Room specifically — inconsistent with the fix just applied to Capsule Room. User was told exactly how to fix it herself (Pause the existing schedule on that checklist's edit page, Add a new one scoped to `Production / Blending Room`, `DAILY`, `SUPERVISOR`, photo not required) but has not done it yet as of this write-up. **Do not silently change it** — see Gotchas below on why automation can't do this one either.

## Key decisions

- **Real controlled documents are built via the live admin UI (Checklist Builder → Workflow Builder → Schedule Manager), never via `prisma/seed.ts` + reseed.** Reseeding production wipes data and is a standing, still-undecided, separate question (see Open Questions) — building content live sidesteps it entirely and is immediately usable. This is *the* pattern for adding any future real checklist content.
- **Checklist versioning is real and load-bearing**: every edit to a checklist's items publishes a new version; historical inspections keep the exact version they were performed against. This is why the user's already-submitted 5S Daily Check inspection from today still shows the old numeric-spinner UI even after the checklist itself was changed to Yes/No/N/A and then to tap-buttons — that's correct behavior, not a bug, and should never be "fixed" by editing in place.
- **Feedback on the 5S scoring UI went through two rounds — the second, more specific round is the one that stuck.** Round 1 ("please mention numbering system" screenshot showing a bare number spinner) was answered by converting to Yes/No/N/A — this was an *overcorrection* that dropped the paper form's real middle "Needs improvement" level. Round 2 ("add number as click system 0 1 2") clarified she wanted the **real 0/1/2 scale kept**, just as tap buttons instead of typing. Final, current state: `NumericScaleButtons` component (`components/inspection/checklist-item-card.tsx`) — any `NUMERIC` item with a bounded range ≤10 renders one button per value (colored critical→warn→pass by position), wider ranges (temperature, humidity) keep the free-text input. **Lesson for next time**: when feedback is about *input friction*, the fix is usually a better input widget, not fewer choices.
- **Single-step "Supervisor Check" workflow** was added (no separate Operator step) specifically because 5S-style checks are performed and signed off by the reviewer themself. Matches existing precedent in this app (see `prisma/seed.ts`'s Capsule Room comment: QA does spot-checks, not per-form sign-off).
- **Checklist cloning copies items but never schedules.** A clone is almost always for a different area than the source; carrying over the source's schedule would just create a duplicate pointing at the same place. This is *why* new clones always start at "0 schedules" and need one added manually — expected, not a bug (see Next steps #1, this confused the user today).
- **Timezone correctness (major thread today)**: Vercel functions run in UTC; this facility is in Queensland (`Australia/Brisbane`, UTC+10, no DST). Every "what day is it" computation across the app used to silently mean the *server's* day, not the *facility's* — for roughly the first 10–14 hours of every real Brisbane day, Today's Ops / the Dashboard / the Calendar could have been showing yesterday's picture. Fixed via a new `lib/timezone.ts` (see Files touched) and applied everywhere date boundaries or displayed timestamps mattered. **This was a real, user-caught bug** ("the topbar says Friday 21 August, my clock already says the 22nd"), not cosmetic.
- **Server-rendered timestamps vs. client-rendered ones are a different bug with a different fix.** `formatAttribution()` (the "Jordan Blake — 19 Aug, 14:32" line under checklist items) runs in `"use client"` components and reads the *viewer's own browser clock* — already correct for a real user physically in Queensland, deliberately left alone. Only *server*-rendered `format()` calls (Audit Trail, Reports, Evidence Gallery, Inspection History, checklist version history) needed fixing, since those bake the server's UTC time into the HTML regardless of who's viewing it.
- **A GitHub multi-account gotcha, confirmed again today**: pushing needs `gh auth switch --hostname github.com --user dhanu-af` first, then switch back to `khdanushka-spec` afterward (the default for her other projects). Every single push this session followed this exact two-command bracket.
- **Push confirmation pattern this session**: after each feature, ask "want this pushed?" before running `git push`, even though `git commit` locally doesn't need to ask. The one time this was skipped (auto-pushing after "next" with no explicit "push" in the same turn) was noted as a one-off deviation, not a new standing permission — go back to asking each time by default.

## Files touched (major ones; `git log --oneline` on this repo has the full, granular history with detailed messages — read those before re-deriving anything)

- **`lib/timezone.ts`** (new) — the timezone-correctness toolkit: `getFacilityTimezone()` (cached per request, reads `Facility.timezone`, falls back to `Australia/Brisbane`), `startOfDayInTimeZone()`/`endOfDayInTimeZone()` (real UTC instants, safe for Prisma `gte`/`lte`), `todayLabelInTimeZone()` (a UTC-midnight *label*, for code like the Calendar grid that already builds its own day sequence as labels rather than real instants), `formatDateInTimeZone()`/`formatDateTimeInTimeZone()`/`formatDayInTimeZone()` (display formatting for server-rendered timestamps). `lib/timezone.test.ts` — 11 unit tests, all passing, hand-verified against Brisbane's fixed +10 offset.
- **`lib/data/{dashboard,inspections,by-category,calendar,reports,audit}.ts`** — all "today"/date-range boundaries now timezone-aware. `calendar.ts` additionally rewritten to use `Date.UTC()`/plain ms-arithmetic instead of `date-fns`' `startOfMonth`/`setDate` (which read the *server process's own* local time — this only coincidentally worked on Vercel's UTC runtime, and was demonstrably wrong when tested on this dev machine, which itself runs in `Australia/Brisbane`).
- **`app/(app)/calendar/page.tsx`**, **`components/calendar/month-grid.tsx`** — same UTC-explicit fix applied to month-navigation and the "Today" cell highlight.
- **`components/nav/topbar.tsx`** — the date header now uses the facility's timezone.
- **`app/(app)/{audit,reports,analytics,evidence,inspections}/page.tsx`**, **`app/(app)/admin/checklists/[id]/page.tsx`** — server-rendered timestamp displays fixed to use the new formatters.
- **`lib/actions/checklist-builder.ts`** — added `cloneChecklist()`. **`components/admin/clone-checklist-button.tsx`** (new) — the Clone icon button, on both the checklist list page and the individual editor header.
- **`components/inspection/checklist-item-card.tsx`** — added `NumericScaleButtons` (tap-button scoring for bounded NUMERIC ranges); also fixed `YES_NO` items to show "YES"/"NO" instead of inherited "PASS"/"FAIL" labels.
- **`lib/permissions.ts`** + **`lib/permissions.test.ts`** (97 tests) — the segregation-of-duties matrix, now with full regression coverage (this was the first test suite in the project; `vitest` was an unused dependency before today).
- **`app/(app)/admin/checklists/{page.tsx,new/*,[id]/*}`**, **`app/(app)/admin/workflows/{page.tsx,new/*,[id]/*}`** — permission gates added (previously reachable by any logged-in user via direct URL), plus the new Verification Workflow create/edit UI (`lib/actions/workflows.ts`, `components/admin/*`).
- **`lib/actions/facility.ts`** — Facility/Section create/archive (mirrors the existing Area/Equipment pattern). **`app/(app)/admin/checklists/[id]/schedule-manager.tsx`** — schedule scope picker widened from area-only to facility/section/area (the backend already supported this; the UI didn't expose it).
- **`lib/notifications.ts`** — `notifyUsers()` batching + per-type extra-recipient routing (`lib/actions/settings.ts`'s `addNotificationRecipient`/`removeNotificationRecipient`, `components/admin/notification-recipients.tsx`).
- **The "5S Daily Check" checklist itself** (production DB content, not code) — 27 items, `NUMERIC` 0–2 with the scoring legend as `helpText` on every item, currently at v1.3 for the original + the "Blending Room" rename, v1.3 for the new "Capsule Room" clone.

## Gotchas / constraints learned this session

- **Local Postgres (`npx prisma dev`) stops between sessions/long gaps — check `netstat -ano | grep 51214` before assuming a code problem.** Happened at least three times today. Fix: `npx prisma dev` from the repo root, comes back on the same port.
- **A concurrent Claude Code session sharing this same `~/webops-platform` directory** was running its own `next dev` (port 3017) most of today. Its in-memory Prisma Client is now **several migrations/schema-generations behind** and will need `taskkill` + `rm -rf .next` + restart before it can touch anything that changed today. Never killed it — always verified changes in an isolated `git worktree` instead (see below).
- **The reliable verification recipe used all session**: `git worktree add ../webops-platform-verify HEAD`, copy `.env`, `ln -s` the real `node_modules` (this can silently take >90s and get moved to background — that's normal, not a hang), `node ../webops-platform/node_modules/prisma/build/index.js generate`, then run `next dev` via `node ../webops-platform/node_modules/next/dist/bin/next dev --port 3019` directly (plain `npx next dev` inside a worktree sometimes fails to resolve symlinked `node_modules` correctly). Always `git worktree remove --force` + `rm -rf` the directory afterward — it can report "device or resource busy" immediately after the process exits, just retry once.
- **The Checklist Builder's item editor has a real, reproducible React state bug under Turbopack dev HMR**: editing many items in one long unsaved session can cause edits past a certain point to silently not persist (confirmed via direct DB queries — not a false alarm). Most likely Fast Refresh partially remounting the component and corrupting the array-index-keyed item list. **Fix that actually worked**: publish incrementally in smaller batches (~14 items, publish, reload, repeat) rather than one long unsaved edit session. Did not reproduce on production (static build, no HMR).
- **`db.facility.findFirst()` with no filter is unsafe** — this repo has an archived leftover test facility ("Second Site Test") alongside the real one, and Postgres gives no ordering guarantee without `orderBy`. A one-off verification script using unfiltered `findFirst()` silently updated the *wrong* facility row today, leaving the real one's timezone stuck on a test value (`America/New_York`) for a while during verification. Always filter `archived: false` (matching `getFacilityTimezone()`'s own convention) or query by explicit id.
- **Browser automation click reliability**: `computer` tool's `left_click` on a `ref` intermittently doesn't fire the handler; `element.click()` via `javascript_tool` is reliable. For React controlled `<input>`/`<select>` values set via script, use the native prototype setter + dispatch both `input` and `change` events (documented pattern, used throughout today's verification scripts).
- **GitHub two-account switch is required before every push**: `gh auth switch --hostname github.com --user dhanu-af`, push, then `gh auth switch --hostname github.com --user khdanushka-spec` back.
- **The auto-mode classifier blocks browser-automation writes that modify existing live production state, but allows ones that create new state**: adding a brand-new schedule to a checklist via the live admin UI went through; clicking "Pause" on an *existing* schedule on a different checklist (same UI, same session, right after) was denied by the classifier. Don't assume one successful production-UI action means the next similar one will also go through — pausing/editing/deleting existing production records may need the user to click it herself.

## Next steps

1. **Blocked on the user, not on decisions**: rescope "5S Daily Check - Blending Room"'s existing schedule from facility-wide to Blending-Room-specific, to match what was just done for Capsule Room. She was given exact click-by-click steps (see State above) and said "ok" but had not acted on it as of this write-up. Don't do this via automation — the classifier blocks pausing/editing existing production schedules (see Gotchas); it only allowed adding the brand-new Capsule Room one. If asked to continue this thread, check whether she's done it yet before re-explaining.
2. Everything else genuinely needs the user's input before proceeding (see Open Questions) — no other clearly-scoped, no-decision-needed work is currently identified. If asked "what's next" again with nothing new from her, the honest answer is these open questions, not another self-directed feature.

## Open questions

- **Reseed production?** Wipes data. Would surface the seed-data-only "C. 5S Check" section and any other seed.ts changes never applied to prod. Still pending from earlier sessions.
- **Two likely-orphaned Vercel projects** (`webops-platform.vercel.app` under the wrong team, `webops-platform-chi.vercel.app` under her personal account) — delete? By whom?
- **Demo accounts** (`admin@webops.demo` etc., password `WebOps2026!`) still live and documented in the login page. Fine pre-launch; flag before this goes in front of anyone outside the team, and consider rotating now that real password-change flows exist.
- **Forgot-password flow** (email + reset token) still doesn't exist — only self-service (needs current password) and admin-initiated reset. Needs real email infrastructure, a bigger separate decision.
- **`SUPERVISOR_REVIEWED`/`QA_REVIEWED` audit actions** are selectable in the Audit Trail filter but never actually logged anywhere — is this meant to be a genuinely separate "reviewed but not yet approved" step, or is it redundant with the existing `SUPERVISOR_APPROVED`/`RETURNED`/`REJECTED` entries? Needs a real design decision, not a guess.
- **`VerificationWorkflow` has no versioning** (unlike checklists) — editing a workflow's steps takes effect immediately for every checklist using it, including in-progress inspections. Not an issue yet; flag if it ever becomes one.
